# Nexus ADB Watchdog 2.1
interval=5
log_rotate_mb=10
status_update=1

# Auto-recovery (ESTABLISHED=0 is NOT a fault)
recovery_enable=1
max_restart=3
restart_window_sec=600
cooldown_sec=60
adbd_sleep_sec=3

# Log only on change/recovery; also heartbeat every N seconds (0=off)
log_heartbeat_sec=300
